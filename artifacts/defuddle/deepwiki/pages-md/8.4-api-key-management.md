# API Key Management

<details>
<summary>Relevant source files</summary>

The following files were used as context for generating this wiki page:

- [.gitignore](.gitignore)
- [website/src/index.ts](website/src/index.ts)
- [website/wrangler.toml](website/wrangler.toml)

</details>



The API Key Management system provides paid access to Defuddle's web service API with higher rate limits than the free IP-based tier. It implements a credit-based system where users purchase blocks of API requests (1,000 / 10,000 / 100,000) through Stripe checkout. The system uses Cloudflare Durable Objects for atomic credit management and idempotent payment processing.

For information about the API endpoints that consume credits, see [API Endpoints](#8.2). For the overall web service architecture, see [Cloudflare Worker Architecture](#8.1).

## System Overview

The API key system consists of four main components:

| Component | Purpose | Implementation |
|-----------|---------|----------------|
| **Key Generation** | Create cryptographically secure API keys | `generateApiKey()` function |
| **Purchase Flow** | Handle Stripe checkout for new keys and top-ups | `createCheckoutFlow()` + session tracking |
| **Credit Management** | Atomic balance operations per API key | `ApiKeyBalanceDO` Durable Object |
| **Webhook Processing** | Idempotent credit allocation from Stripe events | `CheckoutFulfillmentDO` Durable Object |

```mermaid
graph TB
    subgraph "Purchase Initiation"
        ClientRequest["Client Request<br/>POST /api/keys or<br/>POST /api/keys/topup"]
        GenerateKey["generateApiKey()<br/>df_ + 48 hex chars"]
        CreateCheckout["createCheckoutFlow()<br/>Stripe checkout session"]
        SessionTracking["KV Storage<br/>session:token → SessionRecord<br/>stripe_session:id → token"]
    end
    
    subgraph "Payment Processing"
        StripeCheckout["Stripe Checkout<br/>User completes payment"]
        StripeWebhook["POST /api/webhooks/stripe<br/>checkout.session.completed"]
        WebhookVerify["verifyStripeWebhook()<br/>HMAC SHA-256 signature"]
        FulfillmentDO["CheckoutFulfillmentDO<br/>Idempotent processing"]
    end
    
    subgraph "Credit Allocation"
        BalanceDO["ApiKeyBalanceDO<br/>Per-key instance"]
        CreditOperation["credit() method<br/>blockConcurrencyWhile"]
        KVMirror["KV Mirror<br/>key:api_key → balance"]
        SessionUpdate["Update session<br/>status: completed"]
    end
    
    subgraph "Usage"
        ClientPoll["Client polls<br/>GET /api/keys/sessions/{token}"]
        UseAPI["Use API<br/>Authorization: Bearer key"]
        ConsumeCredit["consume() method<br/>Atomic decrement"]
    end
    
    ClientRequest --> GenerateKey
    GenerateKey --> CreateCheckout
    CreateCheckout --> SessionTracking
    SessionTracking --> StripeCheckout
    
    StripeCheckout --> StripeWebhook
    StripeWebhook --> WebhookVerify
    WebhookVerify --> FulfillmentDO
    
    FulfillmentDO --> BalanceDO
    BalanceDO --> CreditOperation
    CreditOperation --> KVMirror
    FulfillmentDO --> SessionUpdate
    
    SessionUpdate --> ClientPoll
    ClientPoll --> UseAPI
    UseAPI --> ConsumeCredit
    ConsumeCredit --> BalanceDO
```

**Sources:** [website/src/index.ts:19-31](), [website/src/index.ts:163-220](), [website/src/index.ts:280-328]()

## API Key Format and Generation

API keys follow a specific format to enable validation and prevent typos. The `generateApiKey()` function produces cryptographically secure keys using the Web Crypto API.

### Key Format

```
df_[48 hexadecimal characters]
```

Example: `df_a1b2c3d4e5f6...` (total length: 51 characters)

The pattern is enforced by `API_KEY_PATTERN` regex: `/^df_[0-9a-f]{48}$/`

### Generation Process

```mermaid
graph LR
    GenerateBytes["crypto.getRandomValues()<br/>24 random bytes"]
    ConvertHex["toHex()<br/>Convert to hex string"]
    AddPrefix["Prepend 'df_'"]
    ValidateFormat["isValidApiKey()<br/>Pattern validation"]
    
    GenerateBytes --> ConvertHex
    ConvertHex --> AddPrefix
    AddPrefix --> ValidateFormat
```

The `toHex()` helper converts byte arrays to hexadecimal strings. A 24-byte random value produces 48 hex characters when encoded.

**Sources:** [website/src/index.ts:161-177](), [website/src/index.ts:94-96]()

## Purchase Blocks and Pricing

The system offers three purchase blocks with different credit allocations:

| Block ID | Requests | Price (USD) | Use Case |
|----------|----------|-------------|----------|
| `1000` | 1,000 | $5.00 | Trial / small projects |
| `10000` | 10,000 | $40.00 | Regular usage |
| `100000` | 100,000 | $300.00 | High-volume applications |

Block definitions are stored in the `BLOCKS` constant as a `Record<string, { requests: number; price: number; name: string }>`.

**Sources:** [website/src/index.ts:19-23]()

## Purchase Flow

### Initial Purchase (New API Key)

When creating a new API key, the flow generates both the key and a session token, then initiates Stripe checkout:

```mermaid
sequenceDiagram
    participant Client
    participant Worker as "Cloudflare Worker"
    participant Stripe
    participant KV as "RATE_LIMIT KV"
    
    Client->>Worker: POST /api/keys<br/>{block: "1000"}
    Worker->>Worker: generateApiKey()<br/>→ df_abc...
    Worker->>Worker: generateSessionToken()<br/>→ 64 hex chars
    Worker->>Worker: hashForMetadata(apiKey)<br/>SHA-256 → 16 chars
    Worker->>Stripe: Create checkout session<br/>metadata: {key_hash, block}
    Stripe-->>Worker: session.id, session.url
    
    par Store Session Data
        Worker->>KV: session:token → SessionRecord<br/>{status, api_key, block, stripe_session_id}
    and Store Reverse Lookup
        Worker->>KV: stripe_session:session.id → token
    end
    
    Worker-->>Client: {checkout_url, session_id: token}
    Client->>Stripe: Navigate to checkout_url
```

The `SessionRecord` type tracks the checkout state:

```typescript
type SessionRecord = {
    status: 'pending' | 'completed';
    api_key: string;
    block: string;
    stripe_session_id: string;
}
```

Session tokens use `generateSessionToken()` which creates 64 hex characters (32 random bytes). Both session records expire after 24 hours (86400 seconds).

**Sources:** [website/src/index.ts:404-412](), [website/src/index.ts:280-328](), [website/src/index.ts:169-173](), [website/src/index.ts:33-38]()

### Top-Up Flow (Existing API Key)

For adding credits to an existing key, the client provides the API key via Bearer authentication:

```mermaid
sequenceDiagram
    participant Client
    participant Worker as "Cloudflare Worker"
    participant BalanceDO as "ApiKeyBalanceDO"
    participant Stripe
    
    Client->>Worker: POST /api/keys/topup<br/>Authorization: Bearer df_abc...<br/>{block: "10000"}
    Worker->>Worker: getBearerApiKey()<br/>Validate format
    Worker->>BalanceDO: status(apiKey)
    BalanceDO-->>Worker: {exists: true, remaining: 350}
    
    alt Key not found
        Worker-->>Client: 404 API key not found
    else Key exists
        Worker->>Worker: createCheckoutFlow(apiKey, block, isTopup=true)
        Worker->>Stripe: Create checkout<br/>metadata: {key_hash, block, topup: "true"}
        Stripe-->>Worker: session.url, session.id
        Worker-->>Client: {checkout_url, session_id}
    end
```

The top-up flow reuses `createCheckoutFlow()` but sets `isTopup: true` in the Stripe metadata and passes the existing `apiKey` instead of generating a new one.

**Sources:** [website/src/index.ts:439-454](), [website/src/index.ts:222-234](), [website/src/index.ts:280-328]()

## Durable Objects Architecture

The system uses two Durable Object classes to provide transactional guarantees and prevent race conditions:

```mermaid
graph TB
    subgraph "ApiKeyBalanceDO"
        AKDO_Instance["One instance per API key<br/>ID from name: apiKey"]
        AKDO_Storage["Durable Storage<br/>{initialized, exists, balance}"]
        AKDO_Methods["Methods:<br/>status(), credit(), consume()"]
        AKDO_KV["KV Mirror<br/>key:apiKey → balance"]
        
        AKDO_Instance --> AKDO_Storage
        AKDO_Instance --> AKDO_Methods
        AKDO_Methods --> AKDO_KV
    end
    
    subgraph "CheckoutFulfillmentDO"
        CFDO_Instance["One instance per Stripe session<br/>ID from name: stripeSessionId"]
        CFDO_Storage["Durable Storage<br/>{processed, sessionToken, apiKey, blockId}"]
        CFDO_Process["process() method<br/>Idempotent credit allocation"]
        
        CFDO_Instance --> CFDO_Storage
        CFDO_Instance --> CFDO_Process
    end
    
    CFDO_Process --> AKDO_Methods
    
    Worker["Cloudflare Worker<br/>fetch handler"] --> AKDO_Instance
    Worker --> CFDO_Instance
```

### ApiKeyBalanceDO

Each API key has its own Durable Object instance providing atomic balance operations. The instance ID is derived from the API key itself using `idFromName(apiKey)`.

#### Storage Schema

| Key | Type | Purpose |
|-----|------|---------|
| `initialized` | `boolean` | Tracks whether storage has been seeded from KV |
| `exists` | `boolean` | Whether this API key has ever been credited |
| `balance` | `number` | Current request count remaining |

#### Methods

**`status(apiKey: string)`** - Returns current balance without modification:

```mermaid
graph LR
    Status_Call["status() called"]
    EnsureInit["ensureInitialized()<br/>Load from KV if first call"]
    ReadStorage["storage.get()<br/>exists, balance"]
    Return["Return ApiKeyMutationResult<br/>{ok, exists, remaining}"]
    
    Status_Call --> EnsureInit
    EnsureInit --> ReadStorage
    ReadStorage --> Return
```

**`credit(apiKey: string, delta: number)`** - Atomically adds credits:

```mermaid
graph LR
    Credit_Call["credit() called"]
    Block["blockConcurrencyWhile()"]
    EnsureInit["ensureInitialized()"]
    Calculate["updated = max(0, current + delta)"]
    WriteStorage["storage.put()<br/>{exists: true, balance: updated}"]
    Mirror["mirrorToKv()<br/>KV.put(key:apiKey, balance)"]
    
    Credit_Call --> Block
    Block --> EnsureInit
    EnsureInit --> Calculate
    Calculate --> WriteStorage
    WriteStorage --> Mirror
```

**`consume(apiKey: string)`** - Atomically decrements balance by 1:

```mermaid
graph LR
    Consume_Call["consume() called"]
    Block["blockConcurrencyWhile()"]
    EnsureInit["ensureInitialized()"]
    CheckExists{exists?}
    CheckBalance{balance > 0?}
    Decrement["updated = current - 1"]
    WriteStorage["storage.put(balance, updated)"]
    Mirror["mirrorToKv()"]
    ReturnOk["Return {ok: true, remaining: updated}"]
    ReturnFail["Return {ok: false}"]
    
    Consume_Call --> Block
    Block --> EnsureInit
    EnsureInit --> CheckExists
    CheckExists -->|No| ReturnFail
    CheckExists -->|Yes| CheckBalance
    CheckBalance -->|No| ReturnFail
    CheckBalance -->|Yes| Decrement
    Decrement --> WriteStorage
    WriteStorage --> Mirror
    Mirror --> ReturnOk
```

All methods use `blockConcurrencyWhile()` to ensure atomic execution. The `mirrorToKv()` function writes the current balance to the `RATE_LIMIT` KV namespace for fast read access without invoking the Durable Object.

**Sources:** [website/src/index.ts:638-763](), [website/src/index.ts:188-190](), [website/src/index.ts:210-220]()

### CheckoutFulfillmentDO

Each Stripe checkout session has its own Durable Object instance providing idempotent payment processing. The instance ID is derived from the Stripe session ID using `idFromName(stripeSessionId)`.

#### Idempotency Guarantee

The `process()` method ensures duplicate webhook deliveries don't credit accounts multiple times:

```mermaid
graph TB
    Process_Call["fetch(/process)<br/>{sessionToken, blockId}"]
    Block["blockConcurrencyWhile()"]
    CheckProcessed{"storage.get('processed')?"}
    ReturnDupe["Return {processed: true,<br/>duplicate: true}"]
    LoadSession["KV.get(session:sessionToken)<br/>→ SessionRecord"]
    Validate["Validate blockId matches"]
    CreditKey["creditApiKey(env, api_key, requests)"]
    UpdateSession["KV.put(session:token)<br/>status = 'completed'"]
    MarkProcessed["storage.put({processed: true,<br/>sessionToken, apiKey, blockId})"]
    ReturnSuccess["Return {processed: true,<br/>duplicate: false}"]
    
    Process_Call --> Block
    Block --> CheckProcessed
    CheckProcessed -->|Yes| ReturnDupe
    CheckProcessed -->|No| LoadSession
    LoadSession --> Validate
    Validate --> CreditKey
    CreditKey --> UpdateSession
    UpdateSession --> MarkProcessed
    MarkProcessed --> ReturnSuccess
```

The stored `processed` flag prevents re-execution even if Stripe sends the webhook multiple times. Once set, subsequent calls immediately return `{processed: true, duplicate: true}`.

**Sources:** [website/src/index.ts:765-822](), [website/src/index.ts:192-194]()

## Webhook Processing

Stripe sends `checkout.session.completed` events to `POST /api/webhooks/stripe` when payments succeed. The system verifies webhook authenticity using HMAC-SHA256 signatures before processing.

### Signature Verification

```mermaid
graph TB
    Webhook["POST /api/webhooks/stripe"]
    ExtractSig["Extract stripe-signature header<br/>Parse timestamp (t) and v1 signatures"]
    CheckTimestamp{"Timestamp within<br/>5 minute window?"}
    ComputeHMAC["Compute HMAC-SHA256<br/>key=webhook_secret<br/>payload=timestamp.body"]
    CompareConstant["constantTimeEquals()<br/>Prevent timing attacks"]
    Valid{"Valid?"}
    Process["Parse event body<br/>Extract metadata"]
    Reject["Return 401 Invalid signature"]
    
    Webhook --> ExtractSig
    ExtractSig --> CheckTimestamp
    CheckTimestamp -->|No| Reject
    CheckTimestamp -->|Yes| ComputeHMAC
    ComputeHMAC --> CompareConstant
    CompareConstant --> Valid
    Valid -->|No| Reject
    Valid -->|Yes| Process
```

The `verifyStripeWebhook()` function implements Stripe's signature scheme:

1. Extract timestamp (`t`) and v1 signature values from the `stripe-signature` header
2. Reject if timestamp is more than 5 minutes old (prevents replay attacks)
3. Construct signed payload: `${timestamp}.${body}`
4. Compute HMAC-SHA256 using `STRIPE_WEBHOOK_SECRET`
5. Compare computed signature against all v1 values using constant-time comparison

The `constantTimeEquals()` function prevents timing-based attacks by ensuring comparison time is independent of where strings differ.

**Sources:** [website/src/index.ts:247-278](), [website/src/index.ts:236-243](), [website/src/index.ts:471-509]()

### Event Processing Flow

```mermaid
sequenceDiagram
    participant Stripe
    participant Worker as "Cloudflare Worker"
    participant KV as "RATE_LIMIT KV"
    participant FulfillDO as "CheckoutFulfillmentDO"
    participant BalanceDO as "ApiKeyBalanceDO"
    
    Stripe->>Worker: POST /api/webhooks/stripe<br/>checkout.session.completed<br/>metadata: {key_hash, block}
    Worker->>Worker: verifyStripeWebhook()<br/>Validate signature
    Worker->>Worker: Parse event.data.object
    Worker->>KV: get(stripe_session:session.id)<br/>→ sessionToken
    Worker->>FulfillDO: fetch(/process)<br/>{sessionToken, blockId}
    
    FulfillDO->>FulfillDO: blockConcurrencyWhile()
    FulfillDO->>FulfillDO: Check if already processed
    
    alt First time processing
        FulfillDO->>KV: get(session:sessionToken)<br/>→ SessionRecord
        FulfillDO->>BalanceDO: credit(apiKey, requests)
        BalanceDO->>BalanceDO: Atomic balance update
        BalanceDO->>KV: put(key:apiKey, newBalance)
        FulfillDO->>KV: put(session:token)<br/>status: "completed"
        FulfillDO->>FulfillDO: storage.put(processed: true)
        FulfillDO-->>Worker: {processed: true, duplicate: false}
    else Already processed
        FulfillDO-->>Worker: {processed: true, duplicate: true}
    end
    
    Worker-->>Stripe: 200 {received: true}
```

The two-level lookup (`stripe_session:id → token → SessionRecord`) allows the webhook to find the correct session data and API key to credit.

**Sources:** [website/src/index.ts:471-509](), [website/src/index.ts:765-822]()

## Authentication Methods

API keys can be provided in two ways when making requests to content conversion endpoints:

### Authorization Header (Recommended)

```
Authorization: Bearer df_a1b2c3d4e5f6...
```

The `getBearerApiKey()` helper extracts and validates the key:

```mermaid
graph LR
    ExtractHeader["Extract Authorization header"]
    CheckPrefix{"Starts with<br/>'Bearer '?"}
    ExtractKey["apiKey = header.slice(7)"]
    ValidateFormat["isValidApiKey(apiKey)"]
    Return["Return {ok: true, apiKey}"]
    Error401["Return {ok: false,<br/>response: 401}"]
    
    ExtractHeader --> CheckPrefix
    CheckPrefix -->|No| Error401
    CheckPrefix -->|Yes| ExtractKey
    ExtractKey --> ValidateFormat
    ValidateFormat -->|Invalid| Error401
    ValidateFormat -->|Valid| Return
```

### Query Parameter

```
GET /example.com/article?key=df_a1b2c3d4e5f6...
```

The worker checks `url.searchParams.has('key')` and extracts the value. This method is less secure since URLs may be logged but is convenient for testing.

**Sources:** [website/src/index.ts:222-234](), [website/src/index.ts:549-557]()

## Credit Consumption

When a request includes an API key, the system atomically consumes one credit before performing work:

```mermaid
sequenceDiagram
    participant Client
    participant Worker as "Cloudflare Worker"
    participant Cache as "Edge Cache"
    participant BalanceDO as "ApiKeyBalanceDO"
    participant Defuddle
    
    Client->>Worker: GET /example.com<br/>Authorization: Bearer df_abc...
    Worker->>Worker: Validate API key format
    
    Worker->>Cache: Check cache (before consuming credit)
    
    alt Cache hit
        Cache-->>Worker: Cached response
        Worker-->>Client: Return cached content<br/>(No credit consumed)
    else Cache miss
        Worker->>BalanceDO: consume(apiKey)
        
        alt Sufficient balance
            BalanceDO->>BalanceDO: Atomic decrement
            BalanceDO->>BalanceDO: mirrorToKv()
            BalanceDO-->>Worker: {ok: true, remaining: N}
            Worker->>Defuddle: Convert content
            Defuddle-->>Worker: Markdown
            Worker->>Cache: Store result
            Worker-->>Client: Return markdown
        else Insufficient balance
            BalanceDO-->>Worker: {ok: false, exists: true,<br/>remaining: 0}
            Worker-->>Client: 402 Payment Required<br/>Purchase more credits
        else Key not found
            BalanceDO-->>Worker: {ok: false, exists: false}
            Worker-->>Client: 404 API key not found
        end
    end
```

Critical ordering:
1. **Cache check before consumption** - Prevents wasting credits on cached content
2. **Atomic consumption before work** - Ensures credit is reserved before expensive operations
3. **Mirror to KV** - Enables fast balance checks without invoking Durable Object

**Sources:** [website/src/index.ts:564-606](), [website/src/index.ts:713-742]()

## Session Polling

After initiating checkout, clients poll for completion using the session token:

```
GET /api/keys/sessions/{sessionToken}
```

### Response States

| Status | HTTP Code | Response Body | Meaning |
|--------|-----------|---------------|---------|
| Pending | 202 | `{status: "pending"}` | Payment not yet completed |
| Completed | 200 | `{status: "completed", api_key: "df_...", remaining: N}` | Credits allocated, key ready |
| Not Found | 404 | `{error: "Session not found."}` | Invalid or expired token |

```mermaid
graph TB
    PollRequest["GET /api/keys/sessions/token"]
    LoadSession["KV.get(session:token)<br/>→ SessionRecord"]
    CheckStatus{"session.status?"}
    CheckBalance["ApiKeyBalanceDO.status()<br/>Get current balance"]
    Pending["Return 202<br/>{status: 'pending'}"]
    Completed["Return 200<br/>{status: 'completed',<br/>api_key, remaining}"]
    NotFound["Return 404<br/>{error: 'Session not found.'}"]
    
    PollRequest --> LoadSession
    LoadSession -->|null| NotFound
    LoadSession -->|exists| CheckStatus
    CheckStatus -->|pending| Pending
    CheckStatus -->|completed| CheckBalance
    CheckBalance --> Completed
```

The webhook handler updates `session.status` from `'pending'` to `'completed'` after successfully crediting the account. Sessions expire after 24 hours.

**Sources:** [website/src/index.ts:414-436]()

## Usage Checking

Clients can check remaining balance at any time:

```
GET /api/keys/usage
Authorization: Bearer df_abc...
```

Response:
```json
{
  "remaining": 9847
}
```

This endpoint invokes `ApiKeyBalanceDO.status()` which reads from durable storage (not the KV mirror), ensuring accuracy even if KV is stale.

**Sources:** [website/src/index.ts:457-468](), [website/src/index.ts:680-692]()

## Configuration

### Environment Variables

The system requires the following bindings in `wrangler.toml`:

| Binding | Type | Purpose |
|---------|------|---------|
| `RATE_LIMIT` | KV Namespace | Session tracking, KV mirrors, IP rate limits |
| `STRIPE_SECRET_KEY` | Secret | Stripe API authentication |
| `STRIPE_WEBHOOK_SECRET` | Secret | Webhook signature verification |
| `API_KEY_BALANCES` | Durable Object | `ApiKeyBalanceDO` namespace |
| `CHECKOUT_FULFILLMENTS` | Durable Object | `CheckoutFulfillmentDO` namespace |

### Durable Object Migrations

The `wrangler.toml` includes a migration tag to initialize SQLite-backed Durable Objects:

```toml
[[migrations]]
tag = "v1"
new_sqlite_classes = ["ApiKeyBalanceDO", "CheckoutFulfillmentDO"]
```

This ensures each Durable Object instance has persistent SQLite storage.

**Sources:** [website/wrangler.toml:1-19](), [website/src/index.ts:25-31]()

## Error Handling

The system returns specific error codes for different failure modes:

| Status | Condition | Message |
|--------|-----------|---------|
| 400 | Invalid block ID | `Invalid block. Options: 1000, 10000, 100000` |
| 401 | Missing/invalid Authorization header | `Missing Authorization: Bearer API key.` or `Invalid API key format.` |
| 402 | Insufficient credits | `API key has no remaining requests. Purchase more at /api/keys or top up at /api/keys/topup` |
| 404 | API key doesn't exist | `API key not found.` |
| 409 | Block mismatch in webhook | `Block mismatch.` |
| 503 | Missing environment configuration | `Payments not configured.` |

All errors return JSON with `{error: "message"}` and appropriate CORS headers.

**Sources:** [website/src/index.ts:121-129](), [website/src/index.ts:571-576]()
