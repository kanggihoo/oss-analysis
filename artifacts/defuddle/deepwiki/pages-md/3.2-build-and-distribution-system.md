# Build and Distribution System

<details>
<summary>Relevant source files</summary>

The following files were used as context for generating this wiki page:

- [package-lock.json](package-lock.json)
- [package.json](package.json)
- [tsconfig.node.json](tsconfig.node.json)
- [webpack.config.js](webpack.config.js)

</details>



This document covers the build configuration, bundling strategy, and distribution system for the Defuddle library. The system produces multiple optimized bundles for different deployment scenarios, from lightweight browser usage to full-featured Node.js integration.

For information about the core extraction logic, see [Core System](#2.1). For details on how individual features are processed, see [Content Standardization](#4).

## Build Architecture

The Defuddle build system uses a multi-target approach, producing three distinct distribution bundles optimized for different use cases. The build process combines TypeScript compilation with Webpack bundling to create browser-compatible UMD modules and Node.js-specific distributions.

```mermaid
graph TB
    subgraph "Source Files"
        SRC["src/index.ts"]
        FULL_SRC["src/index.full.ts"]
        MATH_CORE["src/elements/math.core.ts"]
        MATH_FULL["src/elements/math.full.ts"]
        TS_FILES["src/**/*.ts"]
    end
    
    subgraph "Build Configurations"
        WEBPACK_CONFIG["webpack.config.js"]
        TSCONFIG["tsconfig.json"]
        TSCONFIG_DECL["tsconfig.declarations.json"]
        TSCONFIG_NODE["tsconfig.node.json"]
    end
    
    subgraph "Build Scripts"
        BUILD_TYPES["npm run build:types"]
        BUILD_JS["npm run build:js"]
        BUILD_NODE["npm run build:node"]
        BUILD_ALL["npm run build"]
    end
    
    subgraph "Output Bundles"
        CORE_JS["dist/index.js"]
        CORE_DTS["dist/index.d.ts"]
        FULL_JS["dist/index.full.js"]
        FULL_DTS["dist/index.full.d.ts"]
        NODE_JS["dist/node.js"]
        NODE_DTS["dist/node.d.ts"]
    end
    
    SRC --> BUILD_JS
    FULL_SRC --> BUILD_JS
    TS_FILES --> BUILD_TYPES
    TS_FILES --> BUILD_NODE
    
    WEBPACK_CONFIG --> BUILD_JS
    TSCONFIG_DECL --> BUILD_TYPES
    TSCONFIG_NODE --> BUILD_NODE
    
    BUILD_TYPES --> CORE_DTS
    BUILD_TYPES --> FULL_DTS
    BUILD_JS --> CORE_JS
    BUILD_JS --> FULL_JS
    BUILD_NODE --> NODE_JS
    BUILD_NODE --> NODE_DTS
    
    BUILD_ALL --> BUILD_TYPES
    BUILD_ALL --> BUILD_JS
    BUILD_ALL --> BUILD_NODE
```

Sources: [package.json:31-44](), [webpack.config.js:1-102](), [tsconfig.json:1-16](), [tsconfig.declarations.json:1-20]()

## Bundle Variants

The build system produces three distinct bundle variants, each optimized for specific deployment scenarios and dependency management strategies.

| Bundle | File | Target | Dependencies | Use Case |
|--------|------|--------|--------------|----------|
| Core | `index.js` | Browser | External math libs | Lightweight browser usage |
| Full | `index.full.js` | Browser | All bundled | Self-contained browser deployment |
| Node | `node.js` | Node.js | Optional deps + JSDOM | Server-side processing |

### Core Bundle Configuration

The core bundle uses external dependencies for math processing libraries, keeping the bundle size minimal for browser environments where users may already have these dependencies loaded.

```mermaid
graph LR
    subgraph "Core Bundle Build"
        CORE_ENTRY["src/index.ts"]
        WEBPACK_CORE["coreConfig"]
        MATH_ALIAS["math.core.ts alias"]
        EXTERNALS["externals config"]
    end
    
    subgraph "External Dependencies"
        MATHML_LIB["mathml-to-latex"]
        TEMML_LIB["temml"]
    end
    
    subgraph "Output"
        CORE_UMD["index.js (UMD)"]
        CORE_TYPES["index.d.ts"]
    end
    
    CORE_ENTRY --> WEBPACK_CORE
    MATH_ALIAS --> WEBPACK_CORE
    EXTERNALS --> WEBPACK_CORE
    WEBPACK_CORE --> CORE_UMD
    WEBPACK_CORE --> CORE_TYPES
    
    MATHML_LIB -.->|"External"| CORE_UMD
    TEMML_LIB -.->|"External"| CORE_UMD
```

Sources: [webpack.config.js:47-74](), [package.json:15-20]()

### Full Bundle Configuration

The full bundle includes all dependencies, providing a self-contained distribution for environments where dependency management is challenging or where a single-file deployment is preferred.

```mermaid
graph LR
    subgraph "Full Bundle Build"
        FULL_ENTRY["src/index.full.ts"]
        WEBPACK_FULL["fullConfig"]
        MATH_FULL_ALIAS["math.full.ts alias"]
        NO_EXTERNALS["no externals"]
    end
    
    subgraph "Bundled Dependencies"
        MATHML_BUNDLED["mathml-to-latex bundled"]
        TEMML_BUNDLED["temml bundled"]
    end
    
    subgraph "Output"
        FULL_UMD["index.full.js (UMD)"]
        FULL_TYPES["index.full.d.ts"]
    end
    
    FULL_ENTRY --> WEBPACK_FULL
    MATH_FULL_ALIAS --> WEBPACK_FULL
    NO_EXTERNALS --> WEBPACK_FULL
    WEBPACK_FULL --> FULL_UMD
    WEBPACK_FULL --> FULL_TYPES
    
    MATHML_BUNDLED --> FULL_UMD
    TEMML_BUNDLED --> FULL_UMD
```

Sources: [webpack.config.js:76-99](), [package.json:21-26]()

## TypeScript Compilation Strategy

The build system uses multiple TypeScript configurations to handle different compilation targets and output requirements. Each configuration serves a specific purpose in the overall build pipeline.

### Configuration Files

| Config File | Purpose | Target | Output |
|-------------|---------|--------|---------|
| `tsconfig.json` | Main compilation | CommonJS | Source maps + declarations |
| `tsconfig.declarations.json` | Type definitions only | Multiple entries | `.d.ts` files only |
| `tsconfig.node.json` | Node.js build | Node.js specific | Server-side optimized |

```mermaid
graph TB
    subgraph "TypeScript Configurations"
        MAIN_CONFIG["tsconfig.json"]
        DECL_CONFIG["tsconfig.declarations.json"]
        NODE_CONFIG["tsconfig.node.json"]
    end
    
    subgraph "Compilation Process"
        TSC_MAIN["tsc (main)"]
        TSC_DECL["tsc --project tsconfig.declarations.json"]
        TSC_NODE["tsc --project tsconfig.node.json"]
    end
    
    subgraph "Generated Outputs"
        JS_FILES["CommonJS files"]
        TYPE_DEFS["Type definitions"]
        NODE_FILES["Node.js optimized files"]
        SOURCE_MAPS["Source maps"]
    end
    
    MAIN_CONFIG --> TSC_MAIN
    DECL_CONFIG --> TSC_DECL
    NODE_CONFIG --> TSC_NODE
    
    TSC_MAIN --> JS_FILES
    TSC_MAIN --> SOURCE_MAPS
    TSC_DECL --> TYPE_DEFS
    TSC_NODE --> NODE_FILES
```

Sources: [tsconfig.json:1-16](), [tsconfig.declarations.json:1-20](), [package.json:33-36]()

## Webpack Bundle Configuration

The Webpack configuration creates two parallel builds with different dependency strategies. Both configurations share common settings but differ in their handling of external dependencies and math processing modules.

### Common Configuration Elements

```mermaid
graph TB
    subgraph "Common Webpack Config"
        COMMON_BASE["commonConfig"]
        TS_LOADER["ts-loader"]
        TERSER["TerserPlugin"]
        UMD_FORMAT["UMD library format"]
        GLOBAL_OBJ["globalObject: self/this"]
    end
    
    subgraph "Shared Settings"
        MODE["mode: production/development"]
        DEVTOOL["devtool: source-map"]
        RESOLVE["resolve: .ts/.js extensions"]
        MODULE_IDS["moduleIds: deterministic"]
    end
    
    subgraph "Build Outputs"
        ASCII_OUTPUT["ASCII-only output"]
        DETERMINISTIC["Deterministic builds"]
        MINIFICATION["Production minification"]
    end
    
    COMMON_BASE --> TS_LOADER
    COMMON_BASE --> TERSER
    COMMON_BASE --> UMD_FORMAT
    COMMON_BASE --> GLOBAL_OBJ
    
    MODE --> COMMON_BASE
    DEVTOOL --> COMMON_BASE
    RESOLVE --> COMMON_BASE
    MODULE_IDS --> COMMON_BASE
    
    TERSER --> ASCII_OUTPUT
    MODULE_IDS --> DETERMINISTIC
    TERSER --> MINIFICATION
```

Sources: [webpack.config.js:8-45]()

### Math Module Aliasing

The build system uses Webpack aliases to swap math processing implementations between core and full bundles. This allows the same source code to produce different outputs based on dependency requirements.

| Bundle Type | Alias Target | Math Dependencies |
|-------------|--------------|-------------------|
| Core | `math.core.ts` | External references |
| Full | `math.full.ts` | Bundled implementations |

Sources: [webpack.config.js:69-72](), [webpack.config.js:94-97]()

## Package Export Strategy

The `package.json` exports field defines how different bundles are exposed to consuming applications. This configuration supports both CommonJS and ES module imports while providing appropriate type definitions for each variant.

```mermaid
graph TB
    subgraph "Package Exports"
        MAIN_EXPORT[". (main)"]
        FULL_EXPORT["./full"]
        NODE_EXPORT["./node"]
    end
    
    subgraph "Export Targets"
        CORE_JS["dist/index.js"]
        CORE_TYPES["dist/index.d.ts"]
        FULL_JS["dist/index.full.js"]
        FULL_TYPES["dist/index.full.d.ts"]
        NODE_JS["dist/node.js"]
        NODE_TYPES["dist/node.d.ts"]
    end
    
    subgraph "Import Methods"
        IMPORT_MAIN["import Defuddle from 'defuddle'"]
        IMPORT_FULL["import Defuddle from 'defuddle/full'"]
        IMPORT_NODE["import Defuddle from 'defuddle/node'"]
        REQUIRE_MAIN["require('defuddle')"]
        REQUIRE_FULL["require('defuddle/full')"]
    end
    
    MAIN_EXPORT --> CORE_JS
    MAIN_EXPORT --> CORE_TYPES
    FULL_EXPORT --> FULL_JS
    FULL_EXPORT --> FULL_TYPES
    NODE_EXPORT --> NODE_JS
    NODE_EXPORT --> NODE_TYPES
    
    IMPORT_MAIN --> MAIN_EXPORT
    IMPORT_FULL --> FULL_EXPORT
    IMPORT_NODE --> NODE_EXPORT
    REQUIRE_MAIN --> MAIN_EXPORT
    REQUIRE_FULL --> FULL_EXPORT
```

Sources: [package.json:15-30]()

## Dependency Management

The build system uses a sophisticated dependency management strategy that balances bundle size, functionality, and deployment flexibility through optional and peer dependencies.

### Dependency Categories

| Category | Libraries | Purpose | Bundle Inclusion |
|----------|-----------|---------|------------------|
| `optionalDependencies` | `mathml-to-latex`, `temml`, `turndown` | Math and markdown processing | Conditional |
| `peerDependencies` | `jsdom` | DOM manipulation for Node.js | External |
| `devDependencies` | `webpack`, `typescript`, `vitest` | Build and testing | None |

### Runtime Dependency Resolution

```mermaid
graph TB
    subgraph "Dependency Loading Strategy"
        OPTIONAL_CHECK["Check optional dependencies"]
        PEER_CHECK["Check peer dependencies"]
        FALLBACK["Graceful degradation"]
    end
    
    subgraph "Math Processing"
        MATHML_AVAIL["mathml-to-latex available?"]
        TEMML_AVAIL["temml available?"]
        MATH_FEATURES["Full math support"]
        LIMITED_MATH["Limited math support"]
    end
    
    subgraph "Node.js Integration"
        JSDOM_AVAIL["jsdom available?"]
        DOM_PROCESSING["DOM processing enabled"]
        NO_DOM["Browser-only mode"]
    end
    
    OPTIONAL_CHECK --> MATHML_AVAIL
    OPTIONAL_CHECK --> TEMML_AVAIL
    PEER_CHECK --> JSDOM_AVAIL
    
    MATHML_AVAIL -->|"Yes"| MATH_FEATURES
    MATHML_AVAIL -->|"No"| LIMITED_MATH
    TEMML_AVAIL -->|"Yes"| MATH_FEATURES
    TEMML_AVAIL -->|"No"| LIMITED_MATH
    
    JSDOM_AVAIL -->|"Yes"| DOM_PROCESSING
    JSDOM_AVAIL -->|"No"| NO_DOM
    
    MATH_FEATURES --> FALLBACK
    LIMITED_MATH --> FALLBACK
    DOM_PROCESSING --> FALLBACK
    NO_DOM --> FALLBACK
```

Sources: [package.json:65-72]()

## Build Script Orchestration

The build process is orchestrated through npm scripts that coordinate TypeScript compilation, Webpack bundling, and output generation. The scripts support both development and production workflows.

### Build Commands

```mermaid
graph TB
    subgraph "Build Commands"
        CLEAN["npm run clean"]
        BUILD_TYPES["npm run build:types"]
        BUILD_JS["npm run build:js"]
        BUILD_NODE["npm run build:node"]
        BUILD_ALL["npm run build"]
    end
    
    subgraph "Development Commands"
        DEV_TYPES["npm run dev:types"]
        DEV_JS["npm run dev:js"]
        DEV_NODE["npm run dev:node"]
        DEV_ALL["npm run dev"]
    end
    
    subgraph "File Operations"
        REMOVE_DIST["rm -rf dist"]
        TSC_DECL["tsc --project tsconfig.declarations.json"]
        WEBPACK_BUILD["webpack"]
        TSC_NODE["tsc --project tsconfig.node.json"]
        WATCH_MODE["--watch flags"]
    end
    
    CLEAN --> REMOVE_DIST
    BUILD_TYPES --> TSC_DECL
    BUILD_JS --> WEBPACK_BUILD
    BUILD_NODE --> TSC_NODE
    
    BUILD_ALL --> CLEAN
    BUILD_ALL --> BUILD_TYPES
    BUILD_ALL --> BUILD_NODE
    BUILD_ALL --> BUILD_JS
    
    DEV_TYPES --> TSC_DECL
    DEV_JS --> WEBPACK_BUILD
    DEV_NODE --> TSC_NODE
    DEV_TYPES --> WATCH_MODE
    DEV_JS --> WATCH_MODE
    DEV_NODE --> WATCH_MODE
```

Sources: [package.json:31-44]()

## Output Structure

The build system generates a consistent output structure in the `dist` directory that supports multiple consumption patterns and provides comprehensive type information for TypeScript users.

```
dist/
├── index.js          # Core bundle (UMD)
├── index.d.ts        # Core bundle types
├── index.full.js     # Full bundle (UMD)
├── index.full.d.ts   # Full bundle types
├── node.js          # Node.js bundle
└── node.d.ts        # Node.js bundle types
```

Each bundle includes appropriate source maps in development mode and minified output in production mode, with consistent module IDs to ensure deterministic builds across different environments.

Sources: [package.json:5-14](), [package.json:87-91]()
